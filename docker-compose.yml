services:
  api:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
      - DB_PATH=/app/data/trees.db
    volumes:
      - tree-data:/app/data

volumes:
  tree-data:
